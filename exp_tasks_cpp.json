[
  {
    "id": "S1-A",
    "source": "APPS",
    "group": "algorithmic",
    "difficulty": "junior_ai_10min",
    "title": "원형 보드 게임의 최고 점수 계산",
    "title_ko": "원형 보드 게임의 최고 점수 계산",
    "meta": "origin=apps:1027;estimated_minutes=10",
    "body_kind": "markdown",
    "task_set": 1,
    "origin": "apps:1027",
    "estimated_minutes": 10,
    "language": "cpp17",
    "body": "**사용 언어: C++17**\n\n당신은 모바일 보드게임의 점수 계산 기능을 구현하고 있습니다. 게임 보드는 8개의 구멍이 원형으로 이어진 형태이며, `holes[i]`는 i번째 구멍에 들어 있는 돌의 개수를 나타냅니다.\n\n플레이어는 돌이 하나 이상 들어 있는 구멍 하나를 선택합니다. 선택한 구멍의 돌을 모두 꺼낸 뒤, 바로 다음 구멍부터 시계 방향으로 한 개씩 나누어 놓습니다. 7번 구멍 다음에는 다시 0번 구멍으로 돌아갑니다. 돌의 수가 8개보다 많다면 보드를 여러 바퀴 돌며 계속 분배합니다.\n\n분배가 끝나면 돌이 **양의 짝수 개** 들어 있는 모든 구멍의 돌을 획득하며, 획득한 돌의 총합이 그 턴의 점수입니다. 게임의 힌트 기능에서는 현재 보드에서 한 번 움직였을 때 얻을 수 있는 최고 점수를 보여주어야 합니다.\n\n가능한 모든 시작 구멍을 시험하여 얻을 수 있는 최대 점수를 반환하세요. 시뮬레이션 과정에서 원본 `holes`를 변경하면 이후 게임 화면의 상태가 망가지므로, 입력 리스트는 수정하지 않아야 합니다.\n\n\n\n**C++17 함수 규격**\n```cpp\nlong long solution(std::vector<int> holes);\n```\n\n**입력 형식**\n첫 줄에 8개 정수를 공백으로 구분해 입력합니다.\n```text\n3 1 0 2 4 0 1 2\n```",
    "body_ko": "**사용 언어: C++17**\n\n당신은 모바일 보드게임의 점수 계산 기능을 구현하고 있습니다. 게임 보드는 8개의 구멍이 원형으로 이어진 형태이며, `holes[i]`는 i번째 구멍에 들어 있는 돌의 개수를 나타냅니다.\n\n플레이어는 돌이 하나 이상 들어 있는 구멍 하나를 선택합니다. 선택한 구멍의 돌을 모두 꺼낸 뒤, 바로 다음 구멍부터 시계 방향으로 한 개씩 나누어 놓습니다. 7번 구멍 다음에는 다시 0번 구멍으로 돌아갑니다. 돌의 수가 8개보다 많다면 보드를 여러 바퀴 돌며 계속 분배합니다.\n\n분배가 끝나면 돌이 **양의 짝수 개** 들어 있는 모든 구멍의 돌을 획득하며, 획득한 돌의 총합이 그 턴의 점수입니다. 게임의 힌트 기능에서는 현재 보드에서 한 번 움직였을 때 얻을 수 있는 최고 점수를 보여주어야 합니다.\n\n가능한 모든 시작 구멍을 시험하여 얻을 수 있는 최대 점수를 반환하세요. 시뮬레이션 과정에서 원본 `holes`를 변경하면 이후 게임 화면의 상태가 망가지므로, 입력 리스트는 수정하지 않아야 합니다.\n\n\n\n**C++17 함수 규격**\n```cpp\nlong long solution(std::vector<int> holes);\n```\n\n**입력 형식**\n첫 줄에 8개 정수를 공백으로 구분해 입력합니다.\n```text\n3 1 0 2 4 0 1 2\n```",
    "function_signature": "long long solution(std::vector<int> holes)",
    "starter_code": "#include <bits/stdc++.h>\nusing namespace std;\n\nlong long solution(std::vector<int> holes) {\n    // TODO\n    return 0;\n}",
    "execution_runner": "\n#include <bits/stdc++.h>\nnamespace runner {\nstruct J { enum K{N,B,X,S,A,O}; K k=N; bool b=false; std::string x,s; std::vector<J>a; std::map<std::string,J>o; };\nstruct P { const std::string& z; size_t i=0; void ws(){while(i<z.size()&&isspace((unsigned char)z[i]))++i;} char take(){ws();return i<z.size()?z[i++]:0;} J val(){ char c=take(); if(c=='\"'){J q;q.k=J::S;while(i<z.size()&&z[i]!='\"'){if(z[i]=='\\\\')++i;q.s+=z[i++];}++i;return q;} if(c=='['){J q;q.k=J::A;ws();if(i<z.size()&&z[i]==']'){++i;return q;}for(;;){q.a.push_back(val());ws();char d=z[i++];if(d==']')break;}return q;} if(c=='{'){J q;q.k=J::O;ws();if(i<z.size()&&z[i]=='}'){++i;return q;}for(;;){J key=val();take();q.o[key.s]=val();ws();char d=z[i++];if(d=='}')break;}return q;} --i; size_t st=i;while(i<z.size()&&!strchr(\",]} \\t\\r\\n\",z[i]))++i;std::string t=z.substr(st,i-st);J q;if(t==\"null\")q.k=J::N;else if(t==\"true\"||t==\"false\"){q.k=J::B;q.b=t==\"true\";}else{q.k=J::X;q.x=t;}return q;} };\ninline J read(){std::string l;std::getline(std::cin,l);return P{l}.val();} inline int integer(const J&v){return std::stoi(v.x);} inline double real(const J&v){return std::stod(v.x);} inline std::string str(const J&v){return v.s;} inline bool decimal(const J&v){return v.x.find_first_of(\".eE\")!=std::string::npos;}\ninline std::vector<int> vi(const J&v){std::vector<int>r;for(auto&x:v.a)r.push_back(integer(x));return r;} inline std::vector<std::string> vs(const J&v){std::vector<std::string>r;for(auto&x:v.a)r.push_back(str(x));return r;} inline std::vector<std::vector<int>> vvi(const J&v){std::vector<std::vector<int>>r;for(auto&x:v.a)r.push_back(vi(x));return r;}\ninline std::vector<std::string> split(const std::string&s,char sep){std::vector<std::string>r;std::stringstream q(s);std::string x;while(std::getline(q,x,sep))r.push_back(x);return r;} inline std::vector<int> ints(const std::string&s){std::istringstream q(s);std::vector<int>r;int x;while(q>>x)r.push_back(x);return r;} inline std::vector<std::vector<int>> matrix(const std::string&s){std::vector<std::vector<int>>r;for(auto&row:split(s,';'))r.push_back(ints(row));return r;} inline std::vector<std::string> words(const std::string&s){std::istringstream q(s);std::vector<std::string>r;std::string x;while(q>>x)r.push_back(x);return r;}\ninline std::string esc(std::string s){std::string r;for(char c:s){if(c=='\"'||c=='\\\\')r+='\\\\';r+=c;}return r;} inline std::string out(const std::string&s){return \"\\\"\"+esc(s)+\"\\\"\";} inline std::string out(int x){return std::to_string(x);} inline std::string out(long long x){return std::to_string(x);} inline std::string out(double x){std::ostringstream q;q<<x;return q.str();} inline std::string out(bool x){return x?\"true\":\"false\";} inline std::string out(const std::monostate&){return \"null\";} template<class T> std::string out(const std::vector<T>&v){std::string r=\"[\";for(size_t i=0;i<v.size();++i){if(i)r+=\",\";r+=out(v[i]);}return r+\"]\";}\n}\nint main(){std::string line;std::getline(std::cin,line);std::istringstream in(line);std::vector<int>x;int value;while(in>>value)x.push_back(value);std::cout<<solution(x);}"
  },
  {
    "id": "S1-D",
    "source": "DS-1000",
    "group": "data_manipulation",
    "difficulty": "junior_ai_10min",
    "title": "대시보드 요청 순서대로 측정 기록 구성",
    "title_ko": "대시보드 요청 순서대로 측정 기록 구성",
    "meta": "origin=ds1000:118;estimated_minutes=10",
    "body_kind": "markdown",
    "task_set": 1,
    "origin": "ds1000:118",
    "estimated_minutes": 10,
    "language": "cpp17",
    "body": "**사용 언어: C++17**\n\n당신은 실험 관리 대시보드의 결과 내보내기 기능을 구현하고 있습니다. 서버에서 받은 `rows`에는 참가자 또는 측정 항목별 기록이 구조체 형태로 저장되어 있으며, 각 행에는 고유한 문자열 `id`가 있습니다.\n\n연구자는 보고서에 포함할 기록의 ID를 `wanted_ids`에 원하는 순서대로 입력합니다. 내보내기 결과는 원래 데이터의 정렬 순서가 아니라, `wanted_ids`에 적힌 순서를 따라야 합니다. 요청한 ID가 서버 데이터에 없다면 오류를 발생시키지 말고 해당 ID만 건너뜁니다.\n\n반환되는 리스트와 각 행은 원본과 독립적이어야 합니다. 이후 보고서 생성 과정에서 결과 행에 값을 추가하더라도 서버에서 받은 `rows`가 변경되어서는 안 됩니다.\n\n`wanted_ids`의 순서대로 행을 골라 새로운 리스트로 반환하세요.\n\nC++에서는 각 행을 `Row { string id; int value; }` 구조체로 제한합니다. 요청한 id 순서대로 행을 복사해 반환하세요.\n\n**C++17 함수 규격**\n```cpp\nstd::vector<Row> solution(const std::vector<Row>& rows, const std::vector<std::string>& wanted_ids);\n```\n\n**입력 형식**\n첫 줄은 `id,value` 레코드를 `;`로, 둘째 줄은 원하는 id를 공백으로 구분합니다.\n```text\na,10;b,20\nb a\n```",
    "body_ko": "**사용 언어: C++17**\n\n당신은 실험 관리 대시보드의 결과 내보내기 기능을 구현하고 있습니다. 서버에서 받은 `rows`에는 참가자 또는 측정 항목별 기록이 구조체 형태로 저장되어 있으며, 각 행에는 고유한 문자열 `id`가 있습니다.\n\n연구자는 보고서에 포함할 기록의 ID를 `wanted_ids`에 원하는 순서대로 입력합니다. 내보내기 결과는 원래 데이터의 정렬 순서가 아니라, `wanted_ids`에 적힌 순서를 따라야 합니다. 요청한 ID가 서버 데이터에 없다면 오류를 발생시키지 말고 해당 ID만 건너뜁니다.\n\n반환되는 리스트와 각 행은 원본과 독립적이어야 합니다. 이후 보고서 생성 과정에서 결과 행에 값을 추가하더라도 서버에서 받은 `rows`가 변경되어서는 안 됩니다.\n\n`wanted_ids`의 순서대로 행을 골라 새로운 리스트로 반환하세요.\n\nC++에서는 각 행을 `Row { string id; int value; }` 구조체로 제한합니다. 요청한 id 순서대로 행을 복사해 반환하세요.\n\n**C++17 함수 규격**\n```cpp\nstd::vector<Row> solution(const std::vector<Row>& rows, const std::vector<std::string>& wanted_ids);\n```\n\n**입력 형식**\n첫 줄은 `id,value` 레코드를 `;`로, 둘째 줄은 원하는 id를 공백으로 구분합니다.\n```text\na,10;b,20\nb a\n```",
    "function_signature": "std::vector<Row> solution(const std::vector<Row>& rows, const std::vector<std::string>& wanted_ids)",
    "starter_code": "#include <bits/stdc++.h>\nusing namespace std;\n\nstruct Row { std::string id; int value; };\n\nstd::vector<Row> solution(const std::vector<Row>& rows, const std::vector<std::string>& wanted_ids) {\n    // TODO\n    return {};\n}",
    "execution_runner": "\n#include <bits/stdc++.h>\nnamespace runner {\nstruct J { enum K{N,B,X,S,A,O}; K k=N; bool b=false; std::string x,s; std::vector<J>a; std::map<std::string,J>o; };\nstruct P { const std::string& z; size_t i=0; void ws(){while(i<z.size()&&isspace((unsigned char)z[i]))++i;} char take(){ws();return i<z.size()?z[i++]:0;} J val(){ char c=take(); if(c=='\"'){J q;q.k=J::S;while(i<z.size()&&z[i]!='\"'){if(z[i]=='\\\\')++i;q.s+=z[i++];}++i;return q;} if(c=='['){J q;q.k=J::A;ws();if(i<z.size()&&z[i]==']'){++i;return q;}for(;;){q.a.push_back(val());ws();char d=z[i++];if(d==']')break;}return q;} if(c=='{'){J q;q.k=J::O;ws();if(i<z.size()&&z[i]=='}'){++i;return q;}for(;;){J key=val();take();q.o[key.s]=val();ws();char d=z[i++];if(d=='}')break;}return q;} --i; size_t st=i;while(i<z.size()&&!strchr(\",]} \\t\\r\\n\",z[i]))++i;std::string t=z.substr(st,i-st);J q;if(t==\"null\")q.k=J::N;else if(t==\"true\"||t==\"false\"){q.k=J::B;q.b=t==\"true\";}else{q.k=J::X;q.x=t;}return q;} };\ninline J read(){std::string l;std::getline(std::cin,l);return P{l}.val();} inline int integer(const J&v){return std::stoi(v.x);} inline double real(const J&v){return std::stod(v.x);} inline std::string str(const J&v){return v.s;} inline bool decimal(const J&v){return v.x.find_first_of(\".eE\")!=std::string::npos;}\ninline std::vector<int> vi(const J&v){std::vector<int>r;for(auto&x:v.a)r.push_back(integer(x));return r;} inline std::vector<std::string> vs(const J&v){std::vector<std::string>r;for(auto&x:v.a)r.push_back(str(x));return r;} inline std::vector<std::vector<int>> vvi(const J&v){std::vector<std::vector<int>>r;for(auto&x:v.a)r.push_back(vi(x));return r;}\ninline std::vector<std::string> split(const std::string&s,char sep){std::vector<std::string>r;std::stringstream q(s);std::string x;while(std::getline(q,x,sep))r.push_back(x);return r;} inline std::vector<int> ints(const std::string&s){std::istringstream q(s);std::vector<int>r;int x;while(q>>x)r.push_back(x);return r;} inline std::vector<std::vector<int>> matrix(const std::string&s){std::vector<std::vector<int>>r;for(auto&row:split(s,';'))r.push_back(ints(row));return r;} inline std::vector<std::string> words(const std::string&s){std::istringstream q(s);std::vector<std::string>r;std::string x;while(q>>x)r.push_back(x);return r;}\ninline std::string esc(std::string s){std::string r;for(char c:s){if(c=='\"'||c=='\\\\')r+='\\\\';r+=c;}return r;} inline std::string out(const std::string&s){return \"\\\"\"+esc(s)+\"\\\"\";} inline std::string out(int x){return std::to_string(x);} inline std::string out(long long x){return std::to_string(x);} inline std::string out(double x){std::ostringstream q;q<<x;return q.str();} inline std::string out(bool x){return x?\"true\":\"false\";} inline std::string out(const std::monostate&){return \"null\";} template<class T> std::string out(const std::vector<T>&v){std::string r=\"[\";for(size_t i=0;i<v.size();++i){if(i)r+=\",\";r+=out(v[i]);}return r+\"]\";}\n}\nint main(){std::string line;std::getline(std::cin,line);std::vector<Row>r;for(auto&item:runner::split(line,';')){auto p=runner::split(item,',');r.push_back({p[0],std::stoi(p[1])});}std::getline(std::cin,line);auto w=runner::words(line);auto z=solution(r,w);std::cout<<\"[\";for(size_t i=0;i<z.size();++i){if(i)std::cout<<\",\";std::cout<<\"{\\\"id\\\":\"<<runner::out(z[i].id)<<\",\\\"value\\\":\"<<z[i].value<<\"}\";}std::cout<<\"]\";}"
  },
  {
    "id": "S1-B",
    "source": "BugsInPy",
    "group": "bug_fixing",
    "difficulty": "junior_ai_10min",
    "title": "음성 인식 배치의 길이 정보 shape 오류 수정",
    "title_ko": "음성 인식 배치의 길이 정보 shape 오류 수정",
    "meta": "origin=keras/31;estimated_minutes=10",
    "body_kind": "markdown",
    "task_set": 1,
    "origin": "keras/31",
    "estimated_minutes": 10,
    "language": "cpp17",
    "body": "**사용 언어: C++17**\n\n음성 샘플의 유효 길이는 내부 벡터 하나에 정수 하나가 들어 있는 형태로 전달됩니다. 결과는 항상 `{5, 3, 8}` 같은 1차원 벡터여야 합니다. 그런데 현재 구현은 배치 크기가 1일 때 빈 벡터를 반환합니다. 이 때문에 요청이 한 건만 들어오면 유효 길이 정보가 사라지고, 이후 처리 단계에서 샘플 수와 길이 정보의 개수가 맞지 않게 됩니다.\n\n\n\n**C++17 함수 규격**\n```cpp\nstd::vector<int> solution(const std::vector<std::vector<int>>& lengths);\n```\n\n**입력 형식**\n첫 줄에서 행은 `;`, 각 행의 정수는 공백으로 구분합니다.\n```text\n5;3;8\n```",
    "body_ko": "**사용 언어: C++17**\n\n음성 샘플의 유효 길이는 내부 벡터 하나에 정수 하나가 들어 있는 형태로 전달됩니다. 결과는 항상 `{5, 3, 8}` 같은 1차원 벡터여야 합니다. 그런데 현재 구현은 배치 크기가 1일 때 빈 벡터를 반환합니다. 이 때문에 요청이 한 건만 들어오면 유효 길이 정보가 사라지고, 이후 처리 단계에서 샘플 수와 길이 정보의 개수가 맞지 않게 됩니다.\n\n\n\n**C++17 함수 규격**\n```cpp\nstd::vector<int> solution(const std::vector<std::vector<int>>& lengths);\n```\n\n**입력 형식**\n첫 줄에서 행은 `;`, 각 행의 정수는 공백으로 구분합니다.\n```text\n5;3;8\n```",
    "function_signature": "std::vector<int> solution(const std::vector<std::vector<int>>& lengths)",
    "starter_code": "#include <bits/stdc++.h>\nusing namespace std;\n\nstd::vector<int> solution(const std::vector<std::vector<int>>& lengths) {\n    // BUG: 배치가 하나일 때 빈 결과를 반환합니다.\n    if (lengths.size() == 1) return {};\n    std::vector<int> result;\n    for (const auto& row : lengths) result.push_back(row[0]);\n    return result;\n}",
    "execution_runner": "\n#include <bits/stdc++.h>\nnamespace runner {\nstruct J { enum K{N,B,X,S,A,O}; K k=N; bool b=false; std::string x,s; std::vector<J>a; std::map<std::string,J>o; };\nstruct P { const std::string& z; size_t i=0; void ws(){while(i<z.size()&&isspace((unsigned char)z[i]))++i;} char take(){ws();return i<z.size()?z[i++]:0;} J val(){ char c=take(); if(c=='\"'){J q;q.k=J::S;while(i<z.size()&&z[i]!='\"'){if(z[i]=='\\\\')++i;q.s+=z[i++];}++i;return q;} if(c=='['){J q;q.k=J::A;ws();if(i<z.size()&&z[i]==']'){++i;return q;}for(;;){q.a.push_back(val());ws();char d=z[i++];if(d==']')break;}return q;} if(c=='{'){J q;q.k=J::O;ws();if(i<z.size()&&z[i]=='}'){++i;return q;}for(;;){J key=val();take();q.o[key.s]=val();ws();char d=z[i++];if(d=='}')break;}return q;} --i; size_t st=i;while(i<z.size()&&!strchr(\",]} \\t\\r\\n\",z[i]))++i;std::string t=z.substr(st,i-st);J q;if(t==\"null\")q.k=J::N;else if(t==\"true\"||t==\"false\"){q.k=J::B;q.b=t==\"true\";}else{q.k=J::X;q.x=t;}return q;} };\ninline J read(){std::string l;std::getline(std::cin,l);return P{l}.val();} inline int integer(const J&v){return std::stoi(v.x);} inline double real(const J&v){return std::stod(v.x);} inline std::string str(const J&v){return v.s;} inline bool decimal(const J&v){return v.x.find_first_of(\".eE\")!=std::string::npos;}\ninline std::vector<int> vi(const J&v){std::vector<int>r;for(auto&x:v.a)r.push_back(integer(x));return r;} inline std::vector<std::string> vs(const J&v){std::vector<std::string>r;for(auto&x:v.a)r.push_back(str(x));return r;} inline std::vector<std::vector<int>> vvi(const J&v){std::vector<std::vector<int>>r;for(auto&x:v.a)r.push_back(vi(x));return r;}\ninline std::vector<std::string> split(const std::string&s,char sep){std::vector<std::string>r;std::stringstream q(s);std::string x;while(std::getline(q,x,sep))r.push_back(x);return r;} inline std::vector<int> ints(const std::string&s){std::istringstream q(s);std::vector<int>r;int x;while(q>>x)r.push_back(x);return r;} inline std::vector<std::vector<int>> matrix(const std::string&s){std::vector<std::vector<int>>r;for(auto&row:split(s,';'))r.push_back(ints(row));return r;} inline std::vector<std::string> words(const std::string&s){std::istringstream q(s);std::vector<std::string>r;std::string x;while(q>>x)r.push_back(x);return r;}\ninline std::string esc(std::string s){std::string r;for(char c:s){if(c=='\"'||c=='\\\\')r+='\\\\';r+=c;}return r;} inline std::string out(const std::string&s){return \"\\\"\"+esc(s)+\"\\\"\";} inline std::string out(int x){return std::to_string(x);} inline std::string out(long long x){return std::to_string(x);} inline std::string out(double x){std::ostringstream q;q<<x;return q.str();} inline std::string out(bool x){return x?\"true\":\"false\";} inline std::string out(const std::monostate&){return \"null\";} template<class T> std::string out(const std::vector<T>&v){std::string r=\"[\";for(size_t i=0;i<v.size();++i){if(i)r+=\",\";r+=out(v[i]);}return r+\"]\";}\n}\nint main(){std::string line;std::getline(std::cin,line);std::cout<<runner::out(solution(runner::matrix(line)));}"
  },
  {
    "id": "S2-A",
    "source": "APPS",
    "group": "algorithmic",
    "difficulty": "junior_ai_10min",
    "title": "연속 번호 참가자를 두 팀으로 균형 배정",
    "title_ko": "연속 번호 참가자를 두 팀으로 균형 배정",
    "meta": "origin=apps:1083;estimated_minutes=10",
    "body_kind": "markdown",
    "task_set": 2,
    "origin": "apps:1083",
    "estimated_minutes": 10,
    "language": "cpp17",
    "body": "**사용 언어: C++17**\n\n워크숍 운영팀은 1번부터 `n`번까지 번호가 붙은 참가자들을 두 팀으로 나누려고 합니다. 번호는 참가자의 숙련도 점수를 단순화한 값이며, 두 팀의 번호 합이 비슷할수록 팀 간 균형이 좋다고 가정합니다.\n\n모든 참가자는 정확히 한 팀에 속해야 하고, 두 팀 모두 최소 한 명 이상이어야 합니다. 운영 시스템은 첫 번째 팀에 들어갈 참가자 번호만 저장하며, 나머지 번호는 자동으로 두 번째 팀에 배정합니다.\n\n두 팀의 번호 합 차이가 가능한 한 작아지도록 첫 번째 팀의 번호 목록을 반환하세요. 최소 차이를 만드는 방법이 여러 개라면 그중 아무 것이나 반환해도 됩니다. 채점에서는 반환된 번호의 범위와 중복, 두 팀이 비어 있지 않은지, 그리고 실제 합 차이가 최소인지 확인합니다.\n\n\n\n**C++17 함수 규격**\n```cpp\nstd::vector<int> solution(int n);\n```\n\n**입력 형식**\n첫 줄에 정수 하나를 입력합니다.\n```text\n8\n```",
    "body_ko": "**사용 언어: C++17**\n\n워크숍 운영팀은 1번부터 `n`번까지 번호가 붙은 참가자들을 두 팀으로 나누려고 합니다. 번호는 참가자의 숙련도 점수를 단순화한 값이며, 두 팀의 번호 합이 비슷할수록 팀 간 균형이 좋다고 가정합니다.\n\n모든 참가자는 정확히 한 팀에 속해야 하고, 두 팀 모두 최소 한 명 이상이어야 합니다. 운영 시스템은 첫 번째 팀에 들어갈 참가자 번호만 저장하며, 나머지 번호는 자동으로 두 번째 팀에 배정합니다.\n\n두 팀의 번호 합 차이가 가능한 한 작아지도록 첫 번째 팀의 번호 목록을 반환하세요. 최소 차이를 만드는 방법이 여러 개라면 그중 아무 것이나 반환해도 됩니다. 채점에서는 반환된 번호의 범위와 중복, 두 팀이 비어 있지 않은지, 그리고 실제 합 차이가 최소인지 확인합니다.\n\n\n\n**C++17 함수 규격**\n```cpp\nstd::vector<int> solution(int n);\n```\n\n**입력 형식**\n첫 줄에 정수 하나를 입력합니다.\n```text\n8\n```",
    "function_signature": "std::vector<int> solution(int n)",
    "starter_code": "#include <bits/stdc++.h>\nusing namespace std;\n\nstd::vector<int> solution(int n) {\n    // TODO\n    return {};\n}",
    "execution_runner": "\n#include <bits/stdc++.h>\nnamespace runner {\nstruct J { enum K{N,B,X,S,A,O}; K k=N; bool b=false; std::string x,s; std::vector<J>a; std::map<std::string,J>o; };\nstruct P { const std::string& z; size_t i=0; void ws(){while(i<z.size()&&isspace((unsigned char)z[i]))++i;} char take(){ws();return i<z.size()?z[i++]:0;} J val(){ char c=take(); if(c=='\"'){J q;q.k=J::S;while(i<z.size()&&z[i]!='\"'){if(z[i]=='\\\\')++i;q.s+=z[i++];}++i;return q;} if(c=='['){J q;q.k=J::A;ws();if(i<z.size()&&z[i]==']'){++i;return q;}for(;;){q.a.push_back(val());ws();char d=z[i++];if(d==']')break;}return q;} if(c=='{'){J q;q.k=J::O;ws();if(i<z.size()&&z[i]=='}'){++i;return q;}for(;;){J key=val();take();q.o[key.s]=val();ws();char d=z[i++];if(d=='}')break;}return q;} --i; size_t st=i;while(i<z.size()&&!strchr(\",]} \\t\\r\\n\",z[i]))++i;std::string t=z.substr(st,i-st);J q;if(t==\"null\")q.k=J::N;else if(t==\"true\"||t==\"false\"){q.k=J::B;q.b=t==\"true\";}else{q.k=J::X;q.x=t;}return q;} };\ninline J read(){std::string l;std::getline(std::cin,l);return P{l}.val();} inline int integer(const J&v){return std::stoi(v.x);} inline double real(const J&v){return std::stod(v.x);} inline std::string str(const J&v){return v.s;} inline bool decimal(const J&v){return v.x.find_first_of(\".eE\")!=std::string::npos;}\ninline std::vector<int> vi(const J&v){std::vector<int>r;for(auto&x:v.a)r.push_back(integer(x));return r;} inline std::vector<std::string> vs(const J&v){std::vector<std::string>r;for(auto&x:v.a)r.push_back(str(x));return r;} inline std::vector<std::vector<int>> vvi(const J&v){std::vector<std::vector<int>>r;for(auto&x:v.a)r.push_back(vi(x));return r;}\ninline std::vector<std::string> split(const std::string&s,char sep){std::vector<std::string>r;std::stringstream q(s);std::string x;while(std::getline(q,x,sep))r.push_back(x);return r;} inline std::vector<int> ints(const std::string&s){std::istringstream q(s);std::vector<int>r;int x;while(q>>x)r.push_back(x);return r;} inline std::vector<std::vector<int>> matrix(const std::string&s){std::vector<std::vector<int>>r;for(auto&row:split(s,';'))r.push_back(ints(row));return r;} inline std::vector<std::string> words(const std::string&s){std::istringstream q(s);std::vector<std::string>r;std::string x;while(q>>x)r.push_back(x);return r;}\ninline std::string esc(std::string s){std::string r;for(char c:s){if(c=='\"'||c=='\\\\')r+='\\\\';r+=c;}return r;} inline std::string out(const std::string&s){return \"\\\"\"+esc(s)+\"\\\"\";} inline std::string out(int x){return std::to_string(x);} inline std::string out(long long x){return std::to_string(x);} inline std::string out(double x){std::ostringstream q;q<<x;return q.str();} inline std::string out(bool x){return x?\"true\":\"false\";} inline std::string out(const std::monostate&){return \"null\";} template<class T> std::string out(const std::vector<T>&v){std::string r=\"[\";for(size_t i=0;i<v.size();++i){if(i)r+=\",\";r+=out(v[i]);}return r+\"]\";}\n}\nint main(){int n;std::cin>>n;std::cout<<runner::out(solution(n));}"
  },
  {
    "id": "S2-D",
    "source": "DS-1000",
    "group": "data_manipulation",
    "difficulty": "junior_ai_10min",
    "title": "센서 요청 시점 이후의 첫 측정값 연결",
    "title_ko": "센서 요청 시점 이후의 첫 측정값 연결",
    "meta": "origin=ds1000:108;estimated_minutes=10",
    "body_kind": "markdown",
    "task_set": 2,
    "origin": "ds1000:108",
    "estimated_minutes": 10,
    "language": "cpp17",
    "body": "**사용 언어: C++17**\n\n당신은 서로 다른 두 시스템에서 수집된 시간 기록을 하나의 분석 표로 합치고 있습니다. `queries`에는 분석 요청이 발생한 시점과 요청 정보가 들어 있고, `sources`에는 센서가 실제 값을 기록한 시점과 `data`가 들어 있습니다. 두 목록은 모두 `timestamp` 오름차순으로 정렬되어 있습니다.\n\n각 요청에는 요청 시점과 같거나 그보다 늦게 기록된 센서 값 중 가장 가까운 값을 연결해야 합니다. 즉, 각 query에 대해 `source.timestamp >= query.timestamp`를 만족하는 source 중 timestamp가 가장 작은 기록을 선택합니다. 요청 이후에 기록된 값이 전혀 없다면 `data`는 `std::nullopt`이어야 합니다.\n\n결과 행에는 query가 원래 가지고 있던 모든 키를 유지하고 `data` 키만 추가해야 합니다. 원본 query 구조체는 수정하지 마세요. 목록의 길이가 최대 10,000이므로, 매 query마다 sources 전체를 처음부터 다시 검색하는 방식은 피하는 것이 좋습니다.\n\nC++에서는 `Source`, `Query`, `Result` 구조체를 사용합니다. 해당 센서 값이 없을 때 `Result::data`는 `optional<int>`의 빈 값이어야 합니다.\n\n**C++17 함수 규격**\n```cpp\nstd::vector<Result> solution(const std::vector<Source>& sources, const std::vector<Query>& queries);\n```\n\n**입력 형식**\n각 줄에서 `timestamp,data` 또는 `timestamp,stuff` 레코드를 `;`로 구분합니다.\n```text\n1,100;5,200\n2,7;6,8\n```",
    "body_ko": "**사용 언어: C++17**\n\n당신은 서로 다른 두 시스템에서 수집된 시간 기록을 하나의 분석 표로 합치고 있습니다. `queries`에는 분석 요청이 발생한 시점과 요청 정보가 들어 있고, `sources`에는 센서가 실제 값을 기록한 시점과 `data`가 들어 있습니다. 두 목록은 모두 `timestamp` 오름차순으로 정렬되어 있습니다.\n\n각 요청에는 요청 시점과 같거나 그보다 늦게 기록된 센서 값 중 가장 가까운 값을 연결해야 합니다. 즉, 각 query에 대해 `source.timestamp >= query.timestamp`를 만족하는 source 중 timestamp가 가장 작은 기록을 선택합니다. 요청 이후에 기록된 값이 전혀 없다면 `data`는 `std::nullopt`이어야 합니다.\n\n결과 행에는 query가 원래 가지고 있던 모든 키를 유지하고 `data` 키만 추가해야 합니다. 원본 query 구조체는 수정하지 마세요. 목록의 길이가 최대 10,000이므로, 매 query마다 sources 전체를 처음부터 다시 검색하는 방식은 피하는 것이 좋습니다.\n\nC++에서는 `Source`, `Query`, `Result` 구조체를 사용합니다. 해당 센서 값이 없을 때 `Result::data`는 `optional<int>`의 빈 값이어야 합니다.\n\n**C++17 함수 규격**\n```cpp\nstd::vector<Result> solution(const std::vector<Source>& sources, const std::vector<Query>& queries);\n```\n\n**입력 형식**\n각 줄에서 `timestamp,data` 또는 `timestamp,stuff` 레코드를 `;`로 구분합니다.\n```text\n1,100;5,200\n2,7;6,8\n```",
    "function_signature": "std::vector<Result> solution(const std::vector<Source>& sources, const std::vector<Query>& queries)",
    "starter_code": "#include <bits/stdc++.h>\nusing namespace std;\n\nstruct Source { int timestamp; int data; };\nstruct Query { int timestamp; int stuff; };\nstruct Result { int timestamp; int stuff; std::optional<int> data; };\n\nstd::vector<Result> solution(const std::vector<Source>& sources, const std::vector<Query>& queries) {\n    // TODO\n    return {};\n}",
    "execution_runner": "\n#include <bits/stdc++.h>\nnamespace runner {\nstruct J { enum K{N,B,X,S,A,O}; K k=N; bool b=false; std::string x,s; std::vector<J>a; std::map<std::string,J>o; };\nstruct P { const std::string& z; size_t i=0; void ws(){while(i<z.size()&&isspace((unsigned char)z[i]))++i;} char take(){ws();return i<z.size()?z[i++]:0;} J val(){ char c=take(); if(c=='\"'){J q;q.k=J::S;while(i<z.size()&&z[i]!='\"'){if(z[i]=='\\\\')++i;q.s+=z[i++];}++i;return q;} if(c=='['){J q;q.k=J::A;ws();if(i<z.size()&&z[i]==']'){++i;return q;}for(;;){q.a.push_back(val());ws();char d=z[i++];if(d==']')break;}return q;} if(c=='{'){J q;q.k=J::O;ws();if(i<z.size()&&z[i]=='}'){++i;return q;}for(;;){J key=val();take();q.o[key.s]=val();ws();char d=z[i++];if(d=='}')break;}return q;} --i; size_t st=i;while(i<z.size()&&!strchr(\",]} \\t\\r\\n\",z[i]))++i;std::string t=z.substr(st,i-st);J q;if(t==\"null\")q.k=J::N;else if(t==\"true\"||t==\"false\"){q.k=J::B;q.b=t==\"true\";}else{q.k=J::X;q.x=t;}return q;} };\ninline J read(){std::string l;std::getline(std::cin,l);return P{l}.val();} inline int integer(const J&v){return std::stoi(v.x);} inline double real(const J&v){return std::stod(v.x);} inline std::string str(const J&v){return v.s;} inline bool decimal(const J&v){return v.x.find_first_of(\".eE\")!=std::string::npos;}\ninline std::vector<int> vi(const J&v){std::vector<int>r;for(auto&x:v.a)r.push_back(integer(x));return r;} inline std::vector<std::string> vs(const J&v){std::vector<std::string>r;for(auto&x:v.a)r.push_back(str(x));return r;} inline std::vector<std::vector<int>> vvi(const J&v){std::vector<std::vector<int>>r;for(auto&x:v.a)r.push_back(vi(x));return r;}\ninline std::vector<std::string> split(const std::string&s,char sep){std::vector<std::string>r;std::stringstream q(s);std::string x;while(std::getline(q,x,sep))r.push_back(x);return r;} inline std::vector<int> ints(const std::string&s){std::istringstream q(s);std::vector<int>r;int x;while(q>>x)r.push_back(x);return r;} inline std::vector<std::vector<int>> matrix(const std::string&s){std::vector<std::vector<int>>r;for(auto&row:split(s,';'))r.push_back(ints(row));return r;} inline std::vector<std::string> words(const std::string&s){std::istringstream q(s);std::vector<std::string>r;std::string x;while(q>>x)r.push_back(x);return r;}\ninline std::string esc(std::string s){std::string r;for(char c:s){if(c=='\"'||c=='\\\\')r+='\\\\';r+=c;}return r;} inline std::string out(const std::string&s){return \"\\\"\"+esc(s)+\"\\\"\";} inline std::string out(int x){return std::to_string(x);} inline std::string out(long long x){return std::to_string(x);} inline std::string out(double x){std::ostringstream q;q<<x;return q.str();} inline std::string out(bool x){return x?\"true\":\"false\";} inline std::string out(const std::monostate&){return \"null\";} template<class T> std::string out(const std::vector<T>&v){std::string r=\"[\";for(size_t i=0;i<v.size();++i){if(i)r+=\",\";r+=out(v[i]);}return r+\"]\";}\n}\nint main(){std::string line;std::getline(std::cin,line);std::vector<Source>s;for(auto&item:runner::split(line,';')){auto p=runner::split(item,',');s.push_back({std::stoi(p[0]),std::stoi(p[1])});}std::getline(std::cin,line);std::vector<Query>q;for(auto&item:runner::split(line,';')){auto p=runner::split(item,',');q.push_back({std::stoi(p[0]),std::stoi(p[1])});}auto z=solution(s,q);std::cout<<\"[\";for(size_t i=0;i<z.size();++i){if(i)std::cout<<\",\";std::cout<<\"{\\\"timestamp\\\":\"<<z[i].timestamp<<\",\\\"stuff\\\":\"<<z[i].stuff<<\",\\\"data\\\":\"<<(z[i].data?std::to_string(*z[i].data):\"null\")<<\"}\";}std::cout<<\"]\";}"
  },
  {
    "id": "S2-B",
    "source": "BugsInPy",
    "group": "bug_fixing",
    "difficulty": "junior_ai_10min",
    "title": "이미지 모델 입력 정규화 버그 수정",
    "title_ko": "이미지 모델 입력 정규화 버그 수정",
    "meta": "origin=keras/25;estimated_minutes=10",
    "body_kind": "markdown",
    "task_set": 2,
    "origin": "keras/25",
    "estimated_minutes": 10,
    "language": "cpp17",
    "body": "**사용 언어: C++17**\n\n당신은 이미지 분류 모델에 사진을 전달하기 전 수행하는 전처리 함수를 수정하고 있습니다. 입력 이미지는 0부터 255까지의 정수 픽셀로 구성된 2차원 리스트입니다.\n\n모델은 각 픽셀을 `pixel / 127.5 - 1.0` 공식으로 변환한 -1.0부터 1.0 사이의 실수 값을 기대합니다. 예를 들어 0은 -1.0, 255는 1.0이 되며, 중간 밝기의 픽셀은 소수 값을 가져야 합니다.\n\n현재 코드는 계산 결과를 다시 `int`로 변환해 대부분의 중간값을 0 또는 -1로 만들어 버립니다. 이 상태에서는 서로 다른 밝기의 픽셀이 같은 값으로 입력되어 모델 정확도가 크게 떨어집니다.\n\n원본 이미지와 함수 인터페이스를 유지하면서, 모든 픽셀이 올바른 실수 값으로 변환되도록 버그를 수정하세요.\n\n\n\n**C++17 함수 규격**\n```cpp\nstd::vector<std::vector<double>> solution(const std::vector<std::vector<int>>& image);\n```\n\n**입력 형식**\n첫 줄에서 행은 `;`, 각 행의 픽셀 값은 공백으로 구분합니다.\n```text\n0 255;128 64\n```",
    "body_ko": "**사용 언어: C++17**\n\n당신은 이미지 분류 모델에 사진을 전달하기 전 수행하는 전처리 함수를 수정하고 있습니다. 입력 이미지는 0부터 255까지의 정수 픽셀로 구성된 2차원 리스트입니다.\n\n모델은 각 픽셀을 `pixel / 127.5 - 1.0` 공식으로 변환한 -1.0부터 1.0 사이의 실수 값을 기대합니다. 예를 들어 0은 -1.0, 255는 1.0이 되며, 중간 밝기의 픽셀은 소수 값을 가져야 합니다.\n\n현재 코드는 계산 결과를 다시 `int`로 변환해 대부분의 중간값을 0 또는 -1로 만들어 버립니다. 이 상태에서는 서로 다른 밝기의 픽셀이 같은 값으로 입력되어 모델 정확도가 크게 떨어집니다.\n\n원본 이미지와 함수 인터페이스를 유지하면서, 모든 픽셀이 올바른 실수 값으로 변환되도록 버그를 수정하세요.\n\n\n\n**C++17 함수 규격**\n```cpp\nstd::vector<std::vector<double>> solution(const std::vector<std::vector<int>>& image);\n```\n\n**입력 형식**\n첫 줄에서 행은 `;`, 각 행의 픽셀 값은 공백으로 구분합니다.\n```text\n0 255;128 64\n```",
    "function_signature": "std::vector<std::vector<double>> solution(const std::vector<std::vector<int>>& image)",
    "starter_code": "#include <bits/stdc++.h>\nusing namespace std;\n\nstd::vector<std::vector<double>> solution(const std::vector<std::vector<int>>& image) {\n    std::vector<std::vector<double>> result;\n    for (const auto& row : image) {\n        std::vector<double> converted;\n        for (int pixel : row) {\n            // BUG: 정수 변환이 소수 부분을 버립니다.\n            converted.push_back(static_cast<int>(pixel / 127.5 - 1.0));\n        }\n        result.push_back(converted);\n    }\n    return result;\n}",
    "execution_runner": "\n#include <bits/stdc++.h>\nnamespace runner {\nstruct J { enum K{N,B,X,S,A,O}; K k=N; bool b=false; std::string x,s; std::vector<J>a; std::map<std::string,J>o; };\nstruct P { const std::string& z; size_t i=0; void ws(){while(i<z.size()&&isspace((unsigned char)z[i]))++i;} char take(){ws();return i<z.size()?z[i++]:0;} J val(){ char c=take(); if(c=='\"'){J q;q.k=J::S;while(i<z.size()&&z[i]!='\"'){if(z[i]=='\\\\')++i;q.s+=z[i++];}++i;return q;} if(c=='['){J q;q.k=J::A;ws();if(i<z.size()&&z[i]==']'){++i;return q;}for(;;){q.a.push_back(val());ws();char d=z[i++];if(d==']')break;}return q;} if(c=='{'){J q;q.k=J::O;ws();if(i<z.size()&&z[i]=='}'){++i;return q;}for(;;){J key=val();take();q.o[key.s]=val();ws();char d=z[i++];if(d=='}')break;}return q;} --i; size_t st=i;while(i<z.size()&&!strchr(\",]} \\t\\r\\n\",z[i]))++i;std::string t=z.substr(st,i-st);J q;if(t==\"null\")q.k=J::N;else if(t==\"true\"||t==\"false\"){q.k=J::B;q.b=t==\"true\";}else{q.k=J::X;q.x=t;}return q;} };\ninline J read(){std::string l;std::getline(std::cin,l);return P{l}.val();} inline int integer(const J&v){return std::stoi(v.x);} inline double real(const J&v){return std::stod(v.x);} inline std::string str(const J&v){return v.s;} inline bool decimal(const J&v){return v.x.find_first_of(\".eE\")!=std::string::npos;}\ninline std::vector<int> vi(const J&v){std::vector<int>r;for(auto&x:v.a)r.push_back(integer(x));return r;} inline std::vector<std::string> vs(const J&v){std::vector<std::string>r;for(auto&x:v.a)r.push_back(str(x));return r;} inline std::vector<std::vector<int>> vvi(const J&v){std::vector<std::vector<int>>r;for(auto&x:v.a)r.push_back(vi(x));return r;}\ninline std::vector<std::string> split(const std::string&s,char sep){std::vector<std::string>r;std::stringstream q(s);std::string x;while(std::getline(q,x,sep))r.push_back(x);return r;} inline std::vector<int> ints(const std::string&s){std::istringstream q(s);std::vector<int>r;int x;while(q>>x)r.push_back(x);return r;} inline std::vector<std::vector<int>> matrix(const std::string&s){std::vector<std::vector<int>>r;for(auto&row:split(s,';'))r.push_back(ints(row));return r;} inline std::vector<std::string> words(const std::string&s){std::istringstream q(s);std::vector<std::string>r;std::string x;while(q>>x)r.push_back(x);return r;}\ninline std::string esc(std::string s){std::string r;for(char c:s){if(c=='\"'||c=='\\\\')r+='\\\\';r+=c;}return r;} inline std::string out(const std::string&s){return \"\\\"\"+esc(s)+\"\\\"\";} inline std::string out(int x){return std::to_string(x);} inline std::string out(long long x){return std::to_string(x);} inline std::string out(double x){std::ostringstream q;q<<x;return q.str();} inline std::string out(bool x){return x?\"true\":\"false\";} inline std::string out(const std::monostate&){return \"null\";} template<class T> std::string out(const std::vector<T>&v){std::string r=\"[\";for(size_t i=0;i<v.size();++i){if(i)r+=\",\";r+=out(v[i]);}return r+\"]\";}\n}\nint main(){std::string line;std::getline(std::cin,line);std::cout<<runner::out(solution(runner::matrix(line)));}"
  },
  {
    "id": "S3-A",
    "source": "APPS",
    "group": "algorithmic",
    "difficulty": "junior_ai_10min",
    "title": "레이팅 변동을 절반으로 조정하면서 총합 유지",
    "title_ko": "레이팅 변동을 절반으로 조정하면서 총합 유지",
    "meta": "origin=apps:1078;estimated_minutes=10",
    "body_kind": "markdown",
    "task_set": 3,
    "origin": "apps:1078",
    "estimated_minutes": 10,
    "language": "cpp17",
    "body": "**사용 언어: C++17**\n\n온라인 대회의 레이팅 계산이 끝났지만, 시스템 오류로 이번 대회의 변동 폭을 절반으로 줄여 적용해야 합니다. 원래 참가자별 레이팅 변화량은 `changes`에 들어 있으며, 전체 변화량의 합은 0입니다.\n\n각 변화량을 2로 나눈 결과도 정수로 저장해야 합니다. 원래 값이 짝수라면 정확히 절반을 사용해야 합니다. 홀수라면 정확한 절반이 `.5` 단위가 되므로, 내림값과 올림값 중 하나를 선택할 수 있습니다. 음수의 경우에도 수학적인 내림과 올림을 기준으로 판단해야 합니다.\n\n레이팅 시스템 전체가 균형을 유지하려면 조정된 변화량의 합 역시 0이어야 합니다. 각 참가자의 결과가 허용된 두 반올림 값 중 하나이면서, 전체 합이 0이 되도록 리스트를 만들어 반환하세요. 가능한 답이 여러 개라면 아무 것이나 반환해도 됩니다.\n\n음수 홀수의 반올림 후보를 계산할 때 C++의 `/`는 0 방향 절삭임에 유의하세요. 요구되는 내림·올림 후보를 명시적으로 계산해야 합니다.\n\n**C++17 함수 규격**\n```cpp\nstd::vector<int> solution(const std::vector<int>& changes);\n```\n\n**입력 형식**\n첫 줄에 정수를 공백으로 구분해 입력합니다.\n```text\n-3 5 -2 0\n```",
    "body_ko": "**사용 언어: C++17**\n\n온라인 대회의 레이팅 계산이 끝났지만, 시스템 오류로 이번 대회의 변동 폭을 절반으로 줄여 적용해야 합니다. 원래 참가자별 레이팅 변화량은 `changes`에 들어 있으며, 전체 변화량의 합은 0입니다.\n\n각 변화량을 2로 나눈 결과도 정수로 저장해야 합니다. 원래 값이 짝수라면 정확히 절반을 사용해야 합니다. 홀수라면 정확한 절반이 `.5` 단위가 되므로, 내림값과 올림값 중 하나를 선택할 수 있습니다. 음수의 경우에도 수학적인 내림과 올림을 기준으로 판단해야 합니다.\n\n레이팅 시스템 전체가 균형을 유지하려면 조정된 변화량의 합 역시 0이어야 합니다. 각 참가자의 결과가 허용된 두 반올림 값 중 하나이면서, 전체 합이 0이 되도록 리스트를 만들어 반환하세요. 가능한 답이 여러 개라면 아무 것이나 반환해도 됩니다.\n\n음수 홀수의 반올림 후보를 계산할 때 C++의 `/`는 0 방향 절삭임에 유의하세요. 요구되는 내림·올림 후보를 명시적으로 계산해야 합니다.\n\n**C++17 함수 규격**\n```cpp\nstd::vector<int> solution(const std::vector<int>& changes);\n```\n\n**입력 형식**\n첫 줄에 정수를 공백으로 구분해 입력합니다.\n```text\n-3 5 -2 0\n```",
    "function_signature": "std::vector<int> solution(const std::vector<int>& changes)",
    "starter_code": "#include <bits/stdc++.h>\nusing namespace std;\n\nstd::vector<int> solution(const std::vector<int>& changes) {\n    // TODO\n    return {};\n}",
    "execution_runner": "\n#include <bits/stdc++.h>\nnamespace runner {\nstruct J { enum K{N,B,X,S,A,O}; K k=N; bool b=false; std::string x,s; std::vector<J>a; std::map<std::string,J>o; };\nstruct P { const std::string& z; size_t i=0; void ws(){while(i<z.size()&&isspace((unsigned char)z[i]))++i;} char take(){ws();return i<z.size()?z[i++]:0;} J val(){ char c=take(); if(c=='\"'){J q;q.k=J::S;while(i<z.size()&&z[i]!='\"'){if(z[i]=='\\\\')++i;q.s+=z[i++];}++i;return q;} if(c=='['){J q;q.k=J::A;ws();if(i<z.size()&&z[i]==']'){++i;return q;}for(;;){q.a.push_back(val());ws();char d=z[i++];if(d==']')break;}return q;} if(c=='{'){J q;q.k=J::O;ws();if(i<z.size()&&z[i]=='}'){++i;return q;}for(;;){J key=val();take();q.o[key.s]=val();ws();char d=z[i++];if(d=='}')break;}return q;} --i; size_t st=i;while(i<z.size()&&!strchr(\",]} \\t\\r\\n\",z[i]))++i;std::string t=z.substr(st,i-st);J q;if(t==\"null\")q.k=J::N;else if(t==\"true\"||t==\"false\"){q.k=J::B;q.b=t==\"true\";}else{q.k=J::X;q.x=t;}return q;} };\ninline J read(){std::string l;std::getline(std::cin,l);return P{l}.val();} inline int integer(const J&v){return std::stoi(v.x);} inline double real(const J&v){return std::stod(v.x);} inline std::string str(const J&v){return v.s;} inline bool decimal(const J&v){return v.x.find_first_of(\".eE\")!=std::string::npos;}\ninline std::vector<int> vi(const J&v){std::vector<int>r;for(auto&x:v.a)r.push_back(integer(x));return r;} inline std::vector<std::string> vs(const J&v){std::vector<std::string>r;for(auto&x:v.a)r.push_back(str(x));return r;} inline std::vector<std::vector<int>> vvi(const J&v){std::vector<std::vector<int>>r;for(auto&x:v.a)r.push_back(vi(x));return r;}\ninline std::vector<std::string> split(const std::string&s,char sep){std::vector<std::string>r;std::stringstream q(s);std::string x;while(std::getline(q,x,sep))r.push_back(x);return r;} inline std::vector<int> ints(const std::string&s){std::istringstream q(s);std::vector<int>r;int x;while(q>>x)r.push_back(x);return r;} inline std::vector<std::vector<int>> matrix(const std::string&s){std::vector<std::vector<int>>r;for(auto&row:split(s,';'))r.push_back(ints(row));return r;} inline std::vector<std::string> words(const std::string&s){std::istringstream q(s);std::vector<std::string>r;std::string x;while(q>>x)r.push_back(x);return r;}\ninline std::string esc(std::string s){std::string r;for(char c:s){if(c=='\"'||c=='\\\\')r+='\\\\';r+=c;}return r;} inline std::string out(const std::string&s){return \"\\\"\"+esc(s)+\"\\\"\";} inline std::string out(int x){return std::to_string(x);} inline std::string out(long long x){return std::to_string(x);} inline std::string out(double x){std::ostringstream q;q<<x;return q.str();} inline std::string out(bool x){return x?\"true\":\"false\";} inline std::string out(const std::monostate&){return \"null\";} template<class T> std::string out(const std::vector<T>&v){std::string r=\"[\";for(size_t i=0;i<v.size();++i){if(i)r+=\",\";r+=out(v[i]);}return r+\"]\";}\n}\nint main(){std::string line;std::getline(std::cin,line);std::cout<<runner::out(solution(runner::ints(line)));}"
  },
  {
    "id": "S3-D",
    "source": "DS-1000",
    "group": "data_manipulation",
    "difficulty": "junior_ai_10min",
    "title": "가져온 상품 표의 HTML 토큰 정리",
    "title_ko": "가져온 상품 표의 HTML 토큰 정리",
    "meta": "origin=ds1000:101;estimated_minutes=10",
    "body_kind": "markdown",
    "task_set": 3,
    "origin": "ds1000:101",
    "estimated_minutes": 10,
    "language": "cpp17",
    "body": "**사용 언어: C++17**\n\n당신은 외부 시스템에서 가져온 상품·콘텐츠 표를 정리하는 데이터 파이프라인을 만들고 있습니다. 일부 문자열 필드에서는 HTML 변환 과정의 오류로 비교 기호 `<`가 `&LT;`라는 문자열로 저장되어 있습니다.\n\n표는 구조체 행의 리스트로 주어집니다. 제목, 설명처럼 문자열인 값에는 `&LT;`가 한 번 또는 여러 번 포함될 수 있으며, 이를 모두 실제 문자 `<`로 바꿔야 합니다. 반면 가격, 수량, 활성 여부, 결측값처럼 문자열이 아닌 값은 어떤 변환도 하지 않고 그대로 유지해야 합니다.\n\n정리된 표는 후속 단계에서 별도로 수정될 수 있으므로, 반환 리스트는 원본 `rows`와 다른 객체여야 하고 각 행 구조체도 새로 만들어야 합니다. 원본 표를 직접 수정하지 않도록 주의하세요.\n\nC++에서는 문자열·정수·실수·bool·값 없음이 들어갈 수 있는 `Value` variant를 사용합니다. 문자열 대안에만 `&LT;` 치환을 적용하세요.\n\n**C++17 함수 규격**\n```cpp\nstd::vector<Row> solution(const std::vector<Row>& rows);\n```\n\n**입력 형식**\n첫 줄에서 레코드는 `;`, 필드는 `|`로 구분하고 `키=타입:값`으로 씁니다.\n```text\ntitle=string:hello|count=int:3;active=bool:true\n```",
    "body_ko": "**사용 언어: C++17**\n\n당신은 외부 시스템에서 가져온 상품·콘텐츠 표를 정리하는 데이터 파이프라인을 만들고 있습니다. 일부 문자열 필드에서는 HTML 변환 과정의 오류로 비교 기호 `<`가 `&LT;`라는 문자열로 저장되어 있습니다.\n\n표는 구조체 행의 리스트로 주어집니다. 제목, 설명처럼 문자열인 값에는 `&LT;`가 한 번 또는 여러 번 포함될 수 있으며, 이를 모두 실제 문자 `<`로 바꿔야 합니다. 반면 가격, 수량, 활성 여부, 결측값처럼 문자열이 아닌 값은 어떤 변환도 하지 않고 그대로 유지해야 합니다.\n\n정리된 표는 후속 단계에서 별도로 수정될 수 있으므로, 반환 리스트는 원본 `rows`와 다른 객체여야 하고 각 행 구조체도 새로 만들어야 합니다. 원본 표를 직접 수정하지 않도록 주의하세요.\n\nC++에서는 문자열·정수·실수·bool·값 없음이 들어갈 수 있는 `Value` variant를 사용합니다. 문자열 대안에만 `&LT;` 치환을 적용하세요.\n\n**C++17 함수 규격**\n```cpp\nstd::vector<Row> solution(const std::vector<Row>& rows);\n```\n\n**입력 형식**\n첫 줄에서 레코드는 `;`, 필드는 `|`로 구분하고 `키=타입:값`으로 씁니다.\n```text\ntitle=string:hello|count=int:3;active=bool:true\n```",
    "function_signature": "std::vector<Row> solution(const std::vector<Row>& rows)",
    "starter_code": "#include <bits/stdc++.h>\nusing namespace std;\n\nusing Value = std::variant<std::string, int, double, bool, std::monostate>;\nstruct Row { std::map<std::string, Value> values; };\n\nstd::vector<Row> solution(const std::vector<Row>& rows) {\n    // 문자열인지 확인: std::holds_alternative<std::string>(value)\n    // 문자열 가져오기: std::get<std::string>(value)\n    // TODO: 문자열 대안만 치환하고 나머지 대안은 보존하세요.\n    return {};\n}",
    "execution_runner": "\n#include <bits/stdc++.h>\nnamespace runner {\nstruct J { enum K{N,B,X,S,A,O}; K k=N; bool b=false; std::string x,s; std::vector<J>a; std::map<std::string,J>o; };\nstruct P { const std::string& z; size_t i=0; void ws(){while(i<z.size()&&isspace((unsigned char)z[i]))++i;} char take(){ws();return i<z.size()?z[i++]:0;} J val(){ char c=take(); if(c=='\"'){J q;q.k=J::S;while(i<z.size()&&z[i]!='\"'){if(z[i]=='\\\\')++i;q.s+=z[i++];}++i;return q;} if(c=='['){J q;q.k=J::A;ws();if(i<z.size()&&z[i]==']'){++i;return q;}for(;;){q.a.push_back(val());ws();char d=z[i++];if(d==']')break;}return q;} if(c=='{'){J q;q.k=J::O;ws();if(i<z.size()&&z[i]=='}'){++i;return q;}for(;;){J key=val();take();q.o[key.s]=val();ws();char d=z[i++];if(d=='}')break;}return q;} --i; size_t st=i;while(i<z.size()&&!strchr(\",]} \\t\\r\\n\",z[i]))++i;std::string t=z.substr(st,i-st);J q;if(t==\"null\")q.k=J::N;else if(t==\"true\"||t==\"false\"){q.k=J::B;q.b=t==\"true\";}else{q.k=J::X;q.x=t;}return q;} };\ninline J read(){std::string l;std::getline(std::cin,l);return P{l}.val();} inline int integer(const J&v){return std::stoi(v.x);} inline double real(const J&v){return std::stod(v.x);} inline std::string str(const J&v){return v.s;} inline bool decimal(const J&v){return v.x.find_first_of(\".eE\")!=std::string::npos;}\ninline std::vector<int> vi(const J&v){std::vector<int>r;for(auto&x:v.a)r.push_back(integer(x));return r;} inline std::vector<std::string> vs(const J&v){std::vector<std::string>r;for(auto&x:v.a)r.push_back(str(x));return r;} inline std::vector<std::vector<int>> vvi(const J&v){std::vector<std::vector<int>>r;for(auto&x:v.a)r.push_back(vi(x));return r;}\ninline std::vector<std::string> split(const std::string&s,char sep){std::vector<std::string>r;std::stringstream q(s);std::string x;while(std::getline(q,x,sep))r.push_back(x);return r;} inline std::vector<int> ints(const std::string&s){std::istringstream q(s);std::vector<int>r;int x;while(q>>x)r.push_back(x);return r;} inline std::vector<std::vector<int>> matrix(const std::string&s){std::vector<std::vector<int>>r;for(auto&row:split(s,';'))r.push_back(ints(row));return r;} inline std::vector<std::string> words(const std::string&s){std::istringstream q(s);std::vector<std::string>r;std::string x;while(q>>x)r.push_back(x);return r;}\ninline std::string esc(std::string s){std::string r;for(char c:s){if(c=='\"'||c=='\\\\')r+='\\\\';r+=c;}return r;} inline std::string out(const std::string&s){return \"\\\"\"+esc(s)+\"\\\"\";} inline std::string out(int x){return std::to_string(x);} inline std::string out(long long x){return std::to_string(x);} inline std::string out(double x){std::ostringstream q;q<<x;return q.str();} inline std::string out(bool x){return x?\"true\":\"false\";} inline std::string out(const std::monostate&){return \"null\";} template<class T> std::string out(const std::vector<T>&v){std::string r=\"[\";for(size_t i=0;i<v.size();++i){if(i)r+=\",\";r+=out(v[i]);}return r+\"]\";}\n}\nint main(){std::string line;std::getline(std::cin,line);std::vector<Row>r;for(auto&record:runner::split(line,';')){Row row;for(auto&field:runner::split(record,'|')){auto eq=field.find('=');auto colon=field.find(':',eq+1);auto key=field.substr(0,eq),tag=field.substr(eq+1,colon-eq-1),value=field.substr(colon+1);if(tag==\"int\")row.values[key]=std::stoi(value);else if(tag==\"double\")row.values[key]=std::stod(value);else if(tag==\"string\")row.values[key]=value;else if(tag==\"bool\")row.values[key]=(value==\"true\");else row.values[key]=std::monostate{};}r.push_back(row);}auto z=solution(r);std::cout<<\"[\";for(size_t i=0;i<z.size();++i){if(i)std::cout<<\",\";std::cout<<\"{\";bool f=true;for(auto&kv:z[i].values){if(!f)std::cout<<\",\";f=false;std::cout<<runner::out(kv.first)<<\":\";std::visit([](auto&&q){std::cout<<runner::out(q);},kv.second);}std::cout<<\"}\";}std::cout<<\"]\";}"
  },
  {
    "id": "S3-B",
    "source": "BugsInPy",
    "group": "bug_fixing",
    "difficulty": "junior_ai_10min",
    "title": "코드 포매터의 Python 문법 후보 선택 버그 수정",
    "title_ko": "코드 포매터의 Python 문법 후보 선택 버그 수정",
    "meta": "origin=black/9;estimated_minutes=10",
    "body_kind": "markdown",
    "task_set": 3,
    "origin": "black/9",
    "estimated_minutes": 10,
    "language": "cpp17",
    "body": "**사용 언어: C++17**\n\n당신은 여러 Python 버전의 코드를 처리하는 코드 포매터를 유지보수하고 있습니다. 포매터는 대상 버전에 맞는 문법 파서를 순서대로 시도하며, `solution` 함수는 사용할 파서 후보 이름을 반환합니다.\n\n대상 버전 정보가 없다면 Python 3, 함수형 `print`를 허용하는 Python 2, 일반 Python 2 문법을 모두 시도해야 합니다. 모든 대상이 Python 3 계열이면 Python 3 문법과 함수형 `print` 후보를 사용합니다. Python 2 계열이 하나라도 포함되어 있다면 `print 'x'` 같은 일반 Python 2 문법뿐 아니라 `print('x')`를 함수 호출로 해석할 수 있는 `py2_function_print` 후보도 필요합니다.\n\n현재 구현은 마지막 분기에서 일반 Python 2 후보만 반환합니다. 그 결과 Python 2 프로젝트 안에서 함수형 `print`를 사용하는 파일이 잘못 파싱될 수 있습니다. 기존 반환 순서와 인터페이스를 유지하면서 누락된 후보를 복원하세요.\n\n\n\n**C++17 함수 규격**\n```cpp\nstd::vector<std::string> solution(const std::set<std::string>& target_versions);\n```\n\n**입력 형식**\n첫 줄에 버전 문자열을 공백으로 구분해 입력합니다.\n```text\npy3 py38\n```",
    "body_ko": "**사용 언어: C++17**\n\n당신은 여러 Python 버전의 코드를 처리하는 코드 포매터를 유지보수하고 있습니다. 포매터는 대상 버전에 맞는 문법 파서를 순서대로 시도하며, `solution` 함수는 사용할 파서 후보 이름을 반환합니다.\n\n대상 버전 정보가 없다면 Python 3, 함수형 `print`를 허용하는 Python 2, 일반 Python 2 문법을 모두 시도해야 합니다. 모든 대상이 Python 3 계열이면 Python 3 문법과 함수형 `print` 후보를 사용합니다. Python 2 계열이 하나라도 포함되어 있다면 `print 'x'` 같은 일반 Python 2 문법뿐 아니라 `print('x')`를 함수 호출로 해석할 수 있는 `py2_function_print` 후보도 필요합니다.\n\n현재 구현은 마지막 분기에서 일반 Python 2 후보만 반환합니다. 그 결과 Python 2 프로젝트 안에서 함수형 `print`를 사용하는 파일이 잘못 파싱될 수 있습니다. 기존 반환 순서와 인터페이스를 유지하면서 누락된 후보를 복원하세요.\n\n\n\n**C++17 함수 규격**\n```cpp\nstd::vector<std::string> solution(const std::set<std::string>& target_versions);\n```\n\n**입력 형식**\n첫 줄에 버전 문자열을 공백으로 구분해 입력합니다.\n```text\npy3 py38\n```",
    "function_signature": "std::vector<std::string> solution(const std::set<std::string>& target_versions)",
    "starter_code": "#include <bits/stdc++.h>\nusing namespace std;\n\nstd::vector<std::string> solution(const std::set<std::string>& target_versions) {\n    if (target_versions.empty()) return {\"py3\", \"py2_function_print\", \"py2\"};\n    bool all_py3 = std::all_of(target_versions.begin(), target_versions.end(), [](const std::string& v) { return v.rfind(\"py3\", 0) == 0; });\n    if (all_py3) return {\"py3\", \"py2_function_print\"};\n    // BUG: py2_function_print 후보가 누락되었습니다.\n    return {\"py2\"};\n}",
    "execution_runner": "\n#include <bits/stdc++.h>\nnamespace runner {\nstruct J { enum K{N,B,X,S,A,O}; K k=N; bool b=false; std::string x,s; std::vector<J>a; std::map<std::string,J>o; };\nstruct P { const std::string& z; size_t i=0; void ws(){while(i<z.size()&&isspace((unsigned char)z[i]))++i;} char take(){ws();return i<z.size()?z[i++]:0;} J val(){ char c=take(); if(c=='\"'){J q;q.k=J::S;while(i<z.size()&&z[i]!='\"'){if(z[i]=='\\\\')++i;q.s+=z[i++];}++i;return q;} if(c=='['){J q;q.k=J::A;ws();if(i<z.size()&&z[i]==']'){++i;return q;}for(;;){q.a.push_back(val());ws();char d=z[i++];if(d==']')break;}return q;} if(c=='{'){J q;q.k=J::O;ws();if(i<z.size()&&z[i]=='}'){++i;return q;}for(;;){J key=val();take();q.o[key.s]=val();ws();char d=z[i++];if(d=='}')break;}return q;} --i; size_t st=i;while(i<z.size()&&!strchr(\",]} \\t\\r\\n\",z[i]))++i;std::string t=z.substr(st,i-st);J q;if(t==\"null\")q.k=J::N;else if(t==\"true\"||t==\"false\"){q.k=J::B;q.b=t==\"true\";}else{q.k=J::X;q.x=t;}return q;} };\ninline J read(){std::string l;std::getline(std::cin,l);return P{l}.val();} inline int integer(const J&v){return std::stoi(v.x);} inline double real(const J&v){return std::stod(v.x);} inline std::string str(const J&v){return v.s;} inline bool decimal(const J&v){return v.x.find_first_of(\".eE\")!=std::string::npos;}\ninline std::vector<int> vi(const J&v){std::vector<int>r;for(auto&x:v.a)r.push_back(integer(x));return r;} inline std::vector<std::string> vs(const J&v){std::vector<std::string>r;for(auto&x:v.a)r.push_back(str(x));return r;} inline std::vector<std::vector<int>> vvi(const J&v){std::vector<std::vector<int>>r;for(auto&x:v.a)r.push_back(vi(x));return r;}\ninline std::vector<std::string> split(const std::string&s,char sep){std::vector<std::string>r;std::stringstream q(s);std::string x;while(std::getline(q,x,sep))r.push_back(x);return r;} inline std::vector<int> ints(const std::string&s){std::istringstream q(s);std::vector<int>r;int x;while(q>>x)r.push_back(x);return r;} inline std::vector<std::vector<int>> matrix(const std::string&s){std::vector<std::vector<int>>r;for(auto&row:split(s,';'))r.push_back(ints(row));return r;} inline std::vector<std::string> words(const std::string&s){std::istringstream q(s);std::vector<std::string>r;std::string x;while(q>>x)r.push_back(x);return r;}\ninline std::string esc(std::string s){std::string r;for(char c:s){if(c=='\"'||c=='\\\\')r+='\\\\';r+=c;}return r;} inline std::string out(const std::string&s){return \"\\\"\"+esc(s)+\"\\\"\";} inline std::string out(int x){return std::to_string(x);} inline std::string out(long long x){return std::to_string(x);} inline std::string out(double x){std::ostringstream q;q<<x;return q.str();} inline std::string out(bool x){return x?\"true\":\"false\";} inline std::string out(const std::monostate&){return \"null\";} template<class T> std::string out(const std::vector<T>&v){std::string r=\"[\";for(size_t i=0;i<v.size();++i){if(i)r+=\",\";r+=out(v[i]);}return r+\"]\";}\n}\nint main(){std::string line;std::getline(std::cin,line);auto x=runner::words(line);std::set<std::string>s(x.begin(),x.end());std::cout<<runner::out(solution(s));}"
  },
  {
    "id": "S4-A",
    "source": "APPS",
    "group": "algorithmic",
    "difficulty": "junior_ai_10min",
    "title": "거리 탐지기로 확실히 확인 가능한 대상 계산",
    "title_ko": "거리 탐지기로 확실히 확인 가능한 대상 계산",
    "meta": "origin=apps:1102;estimated_minutes=10",
    "body_kind": "markdown",
    "task_set": 4,
    "origin": "apps:1102",
    "estimated_minutes": 10,
    "language": "cpp17",
    "body": "**사용 언어: C++17**\n\n일렬로 배치된 여러 구역을 감시하는 탐지 시스템이 있습니다. `targets[i]`가 1이면 i번 구역에 대상이 있고, 0이면 없습니다. 관제 요원은 `center` 구역에 있지만, 탐지기는 정확한 위치 대신 중심으로부터 각 거리마다 대상이 몇 명 있는지만 알려줍니다.\n\n같은 거리의 왼쪽과 오른쪽 구역이 모두 존재하는 경우, 탐지 결과만으로 특정 위치를 확신하려면 두 구역에 모두 대상이 있어야 합니다. 한쪽에만 대상이 있으면 어느 쪽인지 구분할 수 없기 때문입니다. 반면 배열 끝에 가까워 같은 거리에 한쪽 구역만 존재한다면, 그 구역의 대상은 위치를 확실히 알 수 있습니다. 중심 구역은 거리 0이므로 대상이 있다면 항상 확인할 수 있습니다.\n\n실제 `targets` 정보가 주어졌을 때, 탐지 결과를 이용해 위치를 확실히 특정할 수 있는 대상의 총수를 반환하세요. `center`는 0부터 시작하는 인덱스입니다.\n\n\n\n**C++17 함수 규격**\n```cpp\nint solution(const std::vector<int>& targets, int center);\n```\n\n**입력 형식**\n첫 줄은 대상 배열, 둘째 줄은 중심 인덱스입니다.\n```text\n1 0 1 1 1\n2\n```",
    "body_ko": "**사용 언어: C++17**\n\n일렬로 배치된 여러 구역을 감시하는 탐지 시스템이 있습니다. `targets[i]`가 1이면 i번 구역에 대상이 있고, 0이면 없습니다. 관제 요원은 `center` 구역에 있지만, 탐지기는 정확한 위치 대신 중심으로부터 각 거리마다 대상이 몇 명 있는지만 알려줍니다.\n\n같은 거리의 왼쪽과 오른쪽 구역이 모두 존재하는 경우, 탐지 결과만으로 특정 위치를 확신하려면 두 구역에 모두 대상이 있어야 합니다. 한쪽에만 대상이 있으면 어느 쪽인지 구분할 수 없기 때문입니다. 반면 배열 끝에 가까워 같은 거리에 한쪽 구역만 존재한다면, 그 구역의 대상은 위치를 확실히 알 수 있습니다. 중심 구역은 거리 0이므로 대상이 있다면 항상 확인할 수 있습니다.\n\n실제 `targets` 정보가 주어졌을 때, 탐지 결과를 이용해 위치를 확실히 특정할 수 있는 대상의 총수를 반환하세요. `center`는 0부터 시작하는 인덱스입니다.\n\n\n\n**C++17 함수 규격**\n```cpp\nint solution(const std::vector<int>& targets, int center);\n```\n\n**입력 형식**\n첫 줄은 대상 배열, 둘째 줄은 중심 인덱스입니다.\n```text\n1 0 1 1 1\n2\n```",
    "function_signature": "int solution(const std::vector<int>& targets, int center)",
    "starter_code": "#include <bits/stdc++.h>\nusing namespace std;\n\nint solution(const std::vector<int>& targets, int center) {\n    // TODO\n    return 0;\n}",
    "execution_runner": "\n#include <bits/stdc++.h>\nnamespace runner {\nstruct J { enum K{N,B,X,S,A,O}; K k=N; bool b=false; std::string x,s; std::vector<J>a; std::map<std::string,J>o; };\nstruct P { const std::string& z; size_t i=0; void ws(){while(i<z.size()&&isspace((unsigned char)z[i]))++i;} char take(){ws();return i<z.size()?z[i++]:0;} J val(){ char c=take(); if(c=='\"'){J q;q.k=J::S;while(i<z.size()&&z[i]!='\"'){if(z[i]=='\\\\')++i;q.s+=z[i++];}++i;return q;} if(c=='['){J q;q.k=J::A;ws();if(i<z.size()&&z[i]==']'){++i;return q;}for(;;){q.a.push_back(val());ws();char d=z[i++];if(d==']')break;}return q;} if(c=='{'){J q;q.k=J::O;ws();if(i<z.size()&&z[i]=='}'){++i;return q;}for(;;){J key=val();take();q.o[key.s]=val();ws();char d=z[i++];if(d=='}')break;}return q;} --i; size_t st=i;while(i<z.size()&&!strchr(\",]} \\t\\r\\n\",z[i]))++i;std::string t=z.substr(st,i-st);J q;if(t==\"null\")q.k=J::N;else if(t==\"true\"||t==\"false\"){q.k=J::B;q.b=t==\"true\";}else{q.k=J::X;q.x=t;}return q;} };\ninline J read(){std::string l;std::getline(std::cin,l);return P{l}.val();} inline int integer(const J&v){return std::stoi(v.x);} inline double real(const J&v){return std::stod(v.x);} inline std::string str(const J&v){return v.s;} inline bool decimal(const J&v){return v.x.find_first_of(\".eE\")!=std::string::npos;}\ninline std::vector<int> vi(const J&v){std::vector<int>r;for(auto&x:v.a)r.push_back(integer(x));return r;} inline std::vector<std::string> vs(const J&v){std::vector<std::string>r;for(auto&x:v.a)r.push_back(str(x));return r;} inline std::vector<std::vector<int>> vvi(const J&v){std::vector<std::vector<int>>r;for(auto&x:v.a)r.push_back(vi(x));return r;}\ninline std::vector<std::string> split(const std::string&s,char sep){std::vector<std::string>r;std::stringstream q(s);std::string x;while(std::getline(q,x,sep))r.push_back(x);return r;} inline std::vector<int> ints(const std::string&s){std::istringstream q(s);std::vector<int>r;int x;while(q>>x)r.push_back(x);return r;} inline std::vector<std::vector<int>> matrix(const std::string&s){std::vector<std::vector<int>>r;for(auto&row:split(s,';'))r.push_back(ints(row));return r;} inline std::vector<std::string> words(const std::string&s){std::istringstream q(s);std::vector<std::string>r;std::string x;while(q>>x)r.push_back(x);return r;}\ninline std::string esc(std::string s){std::string r;for(char c:s){if(c=='\"'||c=='\\\\')r+='\\\\';r+=c;}return r;} inline std::string out(const std::string&s){return \"\\\"\"+esc(s)+\"\\\"\";} inline std::string out(int x){return std::to_string(x);} inline std::string out(long long x){return std::to_string(x);} inline std::string out(double x){std::ostringstream q;q<<x;return q.str();} inline std::string out(bool x){return x?\"true\":\"false\";} inline std::string out(const std::monostate&){return \"null\";} template<class T> std::string out(const std::vector<T>&v){std::string r=\"[\";for(size_t i=0;i<v.size();++i){if(i)r+=\",\";r+=out(v[i]);}return r+\"]\";}\n}\nint main(){std::string line;std::getline(std::cin,line);std::istringstream in(line);std::vector<int>x;int value;while(in>>value)x.push_back(value);std::getline(std::cin,line);std::cout<<solution(x,std::stoi(line));}"
  },
  {
    "id": "S4-D",
    "source": "DS-1000",
    "group": "data_manipulation",
    "difficulty": "junior_ai_10min",
    "title": "혼합 데이터에서 검증된 정수 ID만 추출",
    "title_ko": "혼합 데이터에서 검증된 정수 ID만 추출",
    "meta": "origin=ds1000:113;estimated_minutes=10",
    "body_kind": "markdown",
    "task_set": 4,
    "origin": "ds1000:113",
    "estimated_minutes": 10,
    "language": "cpp17",
    "body": "**사용 언어: C++17**\n\n외부 API 값은 C++ `Value` variant로 전달됩니다. int, double, string, bool, 값 없음 중 실제 int로 태그된 값만 원래 순서대로 추출하세요. C++에서는 int와 bool이 별도 variant 대안이므로, 이 문항은 Python의 bool-정수 상속 함정 대신 variant 타입 태그 판별을 평가합니다.\n\nC++에서는 `Value` variant의 `int` 대안으로 태그된 값만 선택합니다. `bool`과 값 없음은 별도 대안입니다.\n\n**C++17 함수 규격**\n```cpp\nstd::vector<int> solution(const std::vector<Value>& values);\n```\n\n**입력 형식**\n첫 줄에 값 수를, 이후 줄에는 `타입 값`을 입력합니다.\n```text\n3\nint 10\nstring hello\nbool true\n```",
    "body_ko": "**사용 언어: C++17**\n\n외부 API 값은 C++ `Value` variant로 전달됩니다. int, double, string, bool, 값 없음 중 실제 int로 태그된 값만 원래 순서대로 추출하세요. C++에서는 int와 bool이 별도 variant 대안이므로, 이 문항은 Python의 bool-정수 상속 함정 대신 variant 타입 태그 판별을 평가합니다.\n\nC++에서는 `Value` variant의 `int` 대안으로 태그된 값만 선택합니다. `bool`과 값 없음은 별도 대안입니다.\n\n**C++17 함수 규격**\n```cpp\nstd::vector<int> solution(const std::vector<Value>& values);\n```\n\n**입력 형식**\n첫 줄에 값 수를, 이후 줄에는 `타입 값`을 입력합니다.\n```text\n3\nint 10\nstring hello\nbool true\n```",
    "function_signature": "std::vector<int> solution(const std::vector<Value>& values)",
    "starter_code": "#include <bits/stdc++.h>\nusing namespace std;\n\nusing Value = std::variant<int, double, std::string, bool, std::monostate>;\n\nstd::vector<int> solution(const std::vector<Value>& values) {\n    // TODO: int 대안으로 태그된 값만 반환하세요.\n    return {};\n}",
    "execution_runner": "\n#include <bits/stdc++.h>\nnamespace runner {\nstruct J { enum K{N,B,X,S,A,O}; K k=N; bool b=false; std::string x,s; std::vector<J>a; std::map<std::string,J>o; };\nstruct P { const std::string& z; size_t i=0; void ws(){while(i<z.size()&&isspace((unsigned char)z[i]))++i;} char take(){ws();return i<z.size()?z[i++]:0;} J val(){ char c=take(); if(c=='\"'){J q;q.k=J::S;while(i<z.size()&&z[i]!='\"'){if(z[i]=='\\\\')++i;q.s+=z[i++];}++i;return q;} if(c=='['){J q;q.k=J::A;ws();if(i<z.size()&&z[i]==']'){++i;return q;}for(;;){q.a.push_back(val());ws();char d=z[i++];if(d==']')break;}return q;} if(c=='{'){J q;q.k=J::O;ws();if(i<z.size()&&z[i]=='}'){++i;return q;}for(;;){J key=val();take();q.o[key.s]=val();ws();char d=z[i++];if(d=='}')break;}return q;} --i; size_t st=i;while(i<z.size()&&!strchr(\",]} \\t\\r\\n\",z[i]))++i;std::string t=z.substr(st,i-st);J q;if(t==\"null\")q.k=J::N;else if(t==\"true\"||t==\"false\"){q.k=J::B;q.b=t==\"true\";}else{q.k=J::X;q.x=t;}return q;} };\ninline J read(){std::string l;std::getline(std::cin,l);return P{l}.val();} inline int integer(const J&v){return std::stoi(v.x);} inline double real(const J&v){return std::stod(v.x);} inline std::string str(const J&v){return v.s;} inline bool decimal(const J&v){return v.x.find_first_of(\".eE\")!=std::string::npos;}\ninline std::vector<int> vi(const J&v){std::vector<int>r;for(auto&x:v.a)r.push_back(integer(x));return r;} inline std::vector<std::string> vs(const J&v){std::vector<std::string>r;for(auto&x:v.a)r.push_back(str(x));return r;} inline std::vector<std::vector<int>> vvi(const J&v){std::vector<std::vector<int>>r;for(auto&x:v.a)r.push_back(vi(x));return r;}\ninline std::vector<std::string> split(const std::string&s,char sep){std::vector<std::string>r;std::stringstream q(s);std::string x;while(std::getline(q,x,sep))r.push_back(x);return r;} inline std::vector<int> ints(const std::string&s){std::istringstream q(s);std::vector<int>r;int x;while(q>>x)r.push_back(x);return r;} inline std::vector<std::vector<int>> matrix(const std::string&s){std::vector<std::vector<int>>r;for(auto&row:split(s,';'))r.push_back(ints(row));return r;} inline std::vector<std::string> words(const std::string&s){std::istringstream q(s);std::vector<std::string>r;std::string x;while(q>>x)r.push_back(x);return r;}\ninline std::string esc(std::string s){std::string r;for(char c:s){if(c=='\"'||c=='\\\\')r+='\\\\';r+=c;}return r;} inline std::string out(const std::string&s){return \"\\\"\"+esc(s)+\"\\\"\";} inline std::string out(int x){return std::to_string(x);} inline std::string out(long long x){return std::to_string(x);} inline std::string out(double x){std::ostringstream q;q<<x;return q.str();} inline std::string out(bool x){return x?\"true\":\"false\";} inline std::string out(const std::monostate&){return \"null\";} template<class T> std::string out(const std::vector<T>&v){std::string r=\"[\";for(size_t i=0;i<v.size();++i){if(i)r+=\",\";r+=out(v[i]);}return r+\"]\";}\n}\nint main(){std::string line;std::getline(std::cin,line);int count=std::stoi(line);std::vector<Value>v;for(int i=0;i<count;++i){std::getline(std::cin,line);std::istringstream in(line);std::string tag;in>>tag;if(tag==\"int\"){int x;in>>x;v.push_back(x);}else if(tag==\"double\"){double x;in>>x;v.push_back(x);}else if(tag==\"string\"){std::string x;std::getline(in>>std::ws,x);v.push_back(x);}else if(tag==\"bool\"){std::string x;in>>x;v.push_back(x==\"true\");}else if(tag==\"null\")v.push_back(std::monostate{});}std::cout<<runner::out(solution(v));}"
  },
  {
    "id": "S4-B",
    "source": "BugsInPy",
    "group": "bug_fixing",
    "difficulty": "junior_ai_10min",
    "title": "운영체제 메타데이터 변경에 대응하는 Kali 감지",
    "title_ko": "운영체제 메타데이터 변경에 대응하는 Kali 감지",
    "meta": "origin=ansible/3;estimated_minutes=10",
    "body_kind": "markdown",
    "task_set": 4,
    "origin": "ansible/3",
    "estimated_minutes": 10,
    "language": "cpp17",
    "body": "**사용 언어: C++17**\n\n당신은 여러 Linux 서버의 배포판 정보를 수집하는 자동화 도구를 유지보수하고 있습니다. 기존 Kali Linux 환경에서는 `/etc/lsb-release` 파일에 배포판 이름과 버전이 기록되어 있었지만, 최신 환경 중 일부는 표준 `/etc/os-release` 파일만 제공합니다.\n\n함수는 읽어 온 파일의 `path`와 전체 문자열 `data`를 받아 Kali Linux인지 판별해야 합니다. Kali라면 `{'distribution': 'Kali', 'version': ...}` 형태를 반환하고, 다른 배포판이라면 `std::nullopt`을 반환합니다. 버전은 구형 파일의 `DISTRIB_RELEASE=...` 또는 새 파일의 `VERSION_ID=\"...\"` 형식에서 읽어야 합니다.\n\n현재 구현은 `/etc/lsb-release`만 확인하기 때문에 최신 Kali 서버를 알 수 없는 배포판으로 처리합니다. 기존 동작을 깨뜨리지 않으면서 두 경로와 두 버전 표기 방식을 모두 지원하도록 수정하세요. 외부 패키지 없이 표준 `<regex>` 라이브러리만 사용할 수 있습니다.\n\n\n\n**C++17 함수 규격**\n```cpp\nstd::optional<Distribution> solution(const std::string& path, const std::string& data);\n```\n\n**입력 형식**\n첫 줄은 경로, 둘째 줄은 파일 내용을 입력합니다.\n```text\n/etc/lsb-release\nDISTRIB_ID=Kali DISTRIB_RELEASE=2024.1\n```",
    "body_ko": "**사용 언어: C++17**\n\n당신은 여러 Linux 서버의 배포판 정보를 수집하는 자동화 도구를 유지보수하고 있습니다. 기존 Kali Linux 환경에서는 `/etc/lsb-release` 파일에 배포판 이름과 버전이 기록되어 있었지만, 최신 환경 중 일부는 표준 `/etc/os-release` 파일만 제공합니다.\n\n함수는 읽어 온 파일의 `path`와 전체 문자열 `data`를 받아 Kali Linux인지 판별해야 합니다. Kali라면 `{'distribution': 'Kali', 'version': ...}` 형태를 반환하고, 다른 배포판이라면 `std::nullopt`을 반환합니다. 버전은 구형 파일의 `DISTRIB_RELEASE=...` 또는 새 파일의 `VERSION_ID=\"...\"` 형식에서 읽어야 합니다.\n\n현재 구현은 `/etc/lsb-release`만 확인하기 때문에 최신 Kali 서버를 알 수 없는 배포판으로 처리합니다. 기존 동작을 깨뜨리지 않으면서 두 경로와 두 버전 표기 방식을 모두 지원하도록 수정하세요. 외부 패키지 없이 표준 `<regex>` 라이브러리만 사용할 수 있습니다.\n\n\n\n**C++17 함수 규격**\n```cpp\nstd::optional<Distribution> solution(const std::string& path, const std::string& data);\n```\n\n**입력 형식**\n첫 줄은 경로, 둘째 줄은 파일 내용을 입력합니다.\n```text\n/etc/lsb-release\nDISTRIB_ID=Kali DISTRIB_RELEASE=2024.1\n```",
    "function_signature": "std::optional<Distribution> solution(const std::string& path, const std::string& data)",
    "starter_code": "#include <bits/stdc++.h>\nusing namespace std;\n\nstruct Distribution { std::string name; std::optional<std::string> version; };\n\nstd::optional<Distribution> solution(const std::string& path, const std::string& data) {\n    // BUG: 최신 Kali가 사용하는 /etc/os-release를 확인하지 않습니다.\n    if (path == \"/etc/lsb-release\" && data.find(\"Kali\") != std::string::npos) {\n        std::regex pattern(R\"(DISTRIB_RELEASE=\\s*(.*?)\\s*(?:\\n|$))\");\n        std::smatch match;\n        if (std::regex_search(data, match, pattern)) return Distribution{\"Kali\", match[1].str()};\n        return Distribution{\"Kali\", std::nullopt};\n    }\n    return std::nullopt;\n}",
    "execution_runner": "\n#include <bits/stdc++.h>\nnamespace runner {\nstruct J { enum K{N,B,X,S,A,O}; K k=N; bool b=false; std::string x,s; std::vector<J>a; std::map<std::string,J>o; };\nstruct P { const std::string& z; size_t i=0; void ws(){while(i<z.size()&&isspace((unsigned char)z[i]))++i;} char take(){ws();return i<z.size()?z[i++]:0;} J val(){ char c=take(); if(c=='\"'){J q;q.k=J::S;while(i<z.size()&&z[i]!='\"'){if(z[i]=='\\\\')++i;q.s+=z[i++];}++i;return q;} if(c=='['){J q;q.k=J::A;ws();if(i<z.size()&&z[i]==']'){++i;return q;}for(;;){q.a.push_back(val());ws();char d=z[i++];if(d==']')break;}return q;} if(c=='{'){J q;q.k=J::O;ws();if(i<z.size()&&z[i]=='}'){++i;return q;}for(;;){J key=val();take();q.o[key.s]=val();ws();char d=z[i++];if(d=='}')break;}return q;} --i; size_t st=i;while(i<z.size()&&!strchr(\",]} \\t\\r\\n\",z[i]))++i;std::string t=z.substr(st,i-st);J q;if(t==\"null\")q.k=J::N;else if(t==\"true\"||t==\"false\"){q.k=J::B;q.b=t==\"true\";}else{q.k=J::X;q.x=t;}return q;} };\ninline J read(){std::string l;std::getline(std::cin,l);return P{l}.val();} inline int integer(const J&v){return std::stoi(v.x);} inline double real(const J&v){return std::stod(v.x);} inline std::string str(const J&v){return v.s;} inline bool decimal(const J&v){return v.x.find_first_of(\".eE\")!=std::string::npos;}\ninline std::vector<int> vi(const J&v){std::vector<int>r;for(auto&x:v.a)r.push_back(integer(x));return r;} inline std::vector<std::string> vs(const J&v){std::vector<std::string>r;for(auto&x:v.a)r.push_back(str(x));return r;} inline std::vector<std::vector<int>> vvi(const J&v){std::vector<std::vector<int>>r;for(auto&x:v.a)r.push_back(vi(x));return r;}\ninline std::vector<std::string> split(const std::string&s,char sep){std::vector<std::string>r;std::stringstream q(s);std::string x;while(std::getline(q,x,sep))r.push_back(x);return r;} inline std::vector<int> ints(const std::string&s){std::istringstream q(s);std::vector<int>r;int x;while(q>>x)r.push_back(x);return r;} inline std::vector<std::vector<int>> matrix(const std::string&s){std::vector<std::vector<int>>r;for(auto&row:split(s,';'))r.push_back(ints(row));return r;} inline std::vector<std::string> words(const std::string&s){std::istringstream q(s);std::vector<std::string>r;std::string x;while(q>>x)r.push_back(x);return r;}\ninline std::string esc(std::string s){std::string r;for(char c:s){if(c=='\"'||c=='\\\\')r+='\\\\';r+=c;}return r;} inline std::string out(const std::string&s){return \"\\\"\"+esc(s)+\"\\\"\";} inline std::string out(int x){return std::to_string(x);} inline std::string out(long long x){return std::to_string(x);} inline std::string out(double x){std::ostringstream q;q<<x;return q.str();} inline std::string out(bool x){return x?\"true\":\"false\";} inline std::string out(const std::monostate&){return \"null\";} template<class T> std::string out(const std::vector<T>&v){std::string r=\"[\";for(size_t i=0;i<v.size();++i){if(i)r+=\",\";r+=out(v[i]);}return r+\"]\";}\n}\nint main(){std::string p,d;std::getline(std::cin,p);std::getline(std::cin,d);auto z=solution(p,d);if(!z)std::cout<<\"null\";else std::cout<<\"{\\\"name\\\":\"<<runner::out(z->name)<<\",\\\"version\\\":\"<<(z->version?runner::out(*z->version):\"null\")<<\"}\";}"
  }
]
